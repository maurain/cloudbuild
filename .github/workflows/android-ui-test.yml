name: Android UI Automated Tests

on:
  workflow_dispatch:
    inputs:
      profile:
        description: 'Build profile (preview: int release / production: prod release)'
        required: true
        default: 'preview'
        type: choice
        options:
          - preview
          - production
      ref:
        description: 'Target branch / tag / commit SHA in nextie-ai/tuanziclient'
        required: false
        default: 'main'
        type: string
      api-level:
        description: 'Android API Level for emulator'
        required: false
        default: '33'
        type: choice
        options:
          - '31'
          - '33'
          - '34'
      release-tag:
        description: 'Release Tag on nextie-ai/tuanziclient (empty = auto: android-v<buildNumber>)'
        required: false
        default: ''
        type: string

# 并发控制：同一分支的最新提交自动取消旧的构建流程
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-test:
    name: EAS Build & Android Emulator UI Test
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      # 1. 使用 GH_PAT 拉取目标仓库代码
      - name: Checkout target repository
        uses: actions/checkout@v4
        with:
          repository: 'nextie-ai/tuanziclient'
          token: ${{ secrets.GH_PAT || github.token }}
          ref: ${{ github.event.inputs.ref || 'main' }}

      # 2. 配置 Node.js 22.x（EAS CLI 最新依赖要求 Node >= 22）与 npm 缓存
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22.x
          cache: npm

      # 3. 安装项目依赖
      - name: Install dependencies
        run: npm ci --prefer-offline --no-audit

      # 4. 配置 JDK 17 与 Gradle 缓存（EAS local build 与 Maestro 需要）
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'

      # 5. 配置 Gradle 全局仓库过滤（通过 ~/.gradle/init.d/，无需修改项目任何代码与 android 目录）
      # 限定阿里云源仅解析 com.aliyun.* 与 com.alibaba.*，核心 AndroidX / Compose 强制走 Google 官方源，避免海外 Runner 偶发 502
      - name: Configure Gradle repository filtering
        run: |
          mkdir -p ~/.gradle/init.d
          cat << 'EOF' > ~/.gradle/init.d/aliyun-filter.gradle
          allprojects {
            buildscript {
              repositories {
                all { ArtifactRepository repo ->
                  if (repo instanceof MavenArtifactRepository && repo.url.toString().contains("maven.aliyun.com")) {
                    content {
                      includeGroupByRegex "com\\.aliyun.*"
                      includeGroupByRegex "com\\.alibaba.*"
                    }
                  }
                }
              }
            }
            repositories {
              all { ArtifactRepository repo ->
                if (repo instanceof MavenArtifactRepository && repo.url.toString().contains("maven.aliyun.com")) {
                  content {
                    includeGroupByRegex "com\\.aliyun.*"
                    includeGroupByRegex "com\\.alibaba.*"
                  }
                }
              }
            }
          }
          EOF

      # 5. 安装并配置 Expo & EAS CLI
      - name: Setup Expo and EAS
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}
          eas-cache: true
          packager: npm

      # 6. 配置 EAS Android 签名凭据（优先使用 GitHub Secrets 的 Release Keystore，未配置时回退使用 debug.keystore）
      - name: Configure Android signing credentials for EAS
        env:
          KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
          KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
        run: |
          node -e "
          const fs = require('fs');
          const keystoreBase64 = process.env.KEYSTORE_BASE64?.trim();
          let keystoreConfig;

          if (keystoreBase64) {
            console.log('Using Release Keystore from GitHub Secrets...');
            fs.writeFileSync('release.keystore', Buffer.from(keystoreBase64, 'base64'));
            keystoreConfig = {
              keystorePath: 'release.keystore',
              keystorePassword: process.env.KEYSTORE_PASSWORD || '',
              keyAlias: process.env.KEY_ALIAS || '',
              keyPassword: process.env.KEY_PASSWORD || process.env.KEYSTORE_PASSWORD || ''
            };
          } else {
            console.log('ANDROID_KEYSTORE_BASE64 secret not set, falling back to local debug.keystore...');
            keystoreConfig = {
              keystorePath: 'android/app/debug.keystore',
              keystorePassword: 'android',
              keyAlias: 'androiddebugkey',
              keyPassword: 'android'
            };
          }

          fs.writeFileSync('credentials.json', JSON.stringify({ android: { keystore: keystoreConfig } }, null, 2));
          console.log('credentials.json generated successfully.');
          "

      # 7. 自增 EAS Android 版本号（preview 为奇数，production 为偶数）
      - name: Bump EAS build number
        run: |
          PROFILE="${{ github.event.inputs.profile || 'preview' }}"
          node scripts/bump-eas-version.js "$PROFILE" android
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}

      # 8. 使用 EAS 本地命令编译 Android APK
      # 设置 ORG_GRADLE_PROJECT_reactNativeArchitectures 同时编译 x86_64 (模拟器) 与 arm64-v8a (真机)
      - name: Build Android APK Locally via EAS
        run: |
          PROFILE="${{ github.event.inputs.profile || 'preview' }}"
          eas build --platform android --profile "$PROFILE" --local --non-interactive --output=app.apk
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
          ORG_GRADLE_PROJECT_reactNativeArchitectures: 'x86_64,arm64-v8a'

      # 9. 配置 Linux KVM 硬件虚拟化权限，确保模拟器硬件加速正常工作
      - name: Enable KVM permissions
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      # 10. 安装 Maestro UI 自动化测试工具 CLI
      - name: Install Maestro CLI
        run: |
          curl -fsSL "https://get.maestro.mobile.dev" | bash
          echo "$HOME/.maestro/bin" >> $GITHUB_PATH

      # 11. 启动 Android 模拟器并在其中执行自动化测试
      - name: Run Android UI tests in emulator
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: ${{ github.event.inputs.api-level || '33' }}
          target: google_apis
          arch: x86_64
          disable-animations: true
          emulator-options: -no-window -gpu swiftshader_indirect -no-snapshot -noaudio -no-boot-anim -camera-back none
          script: |
            export PATH="$PATH:$HOME/.maestro/bin"

            echo "=== 等待 Android 模拟器准备就绪 ==="
            adb devices
            adb wait-for-device

            echo "=== 安装 EAS 编译生成的 App APK ==="
            adb install -r -d -g app.apk

            echo "=== 验证 App 安装成功 ==="
            adb shell pm list packages | grep com.nextie.tuanzi

            echo "=== 验证 Maestro CLI 运行环境 ==="
            maestro --version

            mkdir -p test-results screenshots

            echo "=== 开始执行 Maestro UI 自动化测试 ==="
            maestro test .maestro/smoke.yaml \
              --format junit \
              --output test-results/maestro-report.xml

      # 12. 测试失败时捕获 Android 系统 logcat 日志便于快速排查定位
      - name: Capture Android logcat on failure
        if: failure()
        run: |
          adb logcat -d > android-logcat.log || true

      # 13. 将编译产物 (APK) 与测试报告发布/上传至目标仓库 nextie-ai/tuanziclient 的 GitHub Release
      - name: Upload APK and artifacts to nextie-ai/tuanziclient
        if: always() && hashFiles('app.apk') != ''
        run: |
          PROFILE="${{ github.event.inputs.profile || 'preview' }}"
          TAG="${{ github.event.inputs.release-tag }}"
          if [ -z "$TAG" ]; then
            TAG="android-v${EAS_BUILD_NUMBER:-${GITHUB_RUN_NUMBER}}"
          fi

          TITLE="Android Build ${EAS_BUILD_NUMBER:-#$GITHUB_RUN_NUMBER} (${PROFILE})"
          NOTES="Automated Android EAS build ($PROFILE) and UI tests run from GitHub Actions."

          echo "Target repo: nextie-ai/tuanziclient, Tag: $TAG"

          if gh release view "$TAG" --repo nextie-ai/tuanziclient >/dev/null 2>&1; then
            echo "Release $TAG already exists. Uploading/updating assets..."
            gh release upload "$TAG" app.apk --repo nextie-ai/tuanziclient --clobber
            if [ -f test-results/maestro-report.xml ]; then
              gh release upload "$TAG" test-results/maestro-report.xml --repo nextie-ai/tuanziclient --clobber || true
            fi
          else
            echo "Creating new release $TAG on nextie-ai/tuanziclient..."
            gh release create "$TAG" app.apk \
              --repo nextie-ai/tuanziclient \
              --title "$TITLE" \
              --notes "$NOTES" \
              --target "${{ github.event.inputs.ref || 'main' }}" \
              --prerelease
            if [ -f test-results/maestro-report.xml ]; then
              gh release upload "$TAG" test-results/maestro-report.xml --repo nextie-ai/tuanziclient --clobber || true
            fi
          fi
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}

      # 14. 同时将测试产物上传至当前 Runner 工作流（供网页直接下载查看截屏与日志）
      - name: Upload local workflow artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: maestro-test-results
          path: |
            test-results/
            screenshots/
            ~/.maestro/tests/
            android-logcat.log
            app.apk
          retention-days: 7
